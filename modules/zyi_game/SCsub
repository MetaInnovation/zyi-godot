# SCsub
from misc.utility.scons_hints import *

Import("env")
Import("env_modules")

env_zyi_game = env_modules.Clone()

env_zyi_game.add_source_files(env.modules_sources, "*.cpp")  # Add all cpp files to the build
env_zyi_game.add_source_files(
    env.modules_sources, "./util/*.cpp"
)  # Add all cpp files to the build
env_zyi_game.add_source_files(
    env.modules_sources, "./damage_core/*.cpp"
)  # Add all cpp files to the build
env_zyi_game.add_source_files(
    env.modules_sources, "./pool/*.cpp"
)  # Add all cpp files to the build
